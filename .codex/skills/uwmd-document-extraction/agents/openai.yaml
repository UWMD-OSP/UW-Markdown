interface:
  display_name: "UWMD Document Extraction,short_description=Extract source facts as structured edits.,default_prompt=Extract cited source facts and emit structured UWMD edits only."
  short_description: "UWMD Document Extraction,short_description=Extract source helper"
