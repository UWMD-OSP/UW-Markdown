# Release 2.9.0 verification

Verified 2026-09-12. The [release PR #191](https://github.com/UWMD-OSP/UW-Markdown/pull/191)
passed CI before merge. Tag `v2.9.0` points to `7d939c7044e17ffbded7ec939edc9d0be8d35b87`.
The [trusted-publishing run](https://github.com/UWMD-OSP/UW-Markdown/actions/runs/34722139177)
completed successfully using the existing OIDC workflow.

## Published pairing

| Package | npm version |
|---|---|
| `@uwmd/core` | 2.9.0 |
| `@uwmd/cli` | 2.9.0 |
| `@uwmd/signing` | 0.2.13 |
| `@uwmd/batch` | 0.8.8 |

Protocol is 2.12.0 and Format is 2.0. Excel 0.9.1, report 0.8.13 and the
0.1.1 module packages remain unpublished. Module manifests retain their
independent 0.1.0 contract version. External dependencies and workspace links
are unchanged. The three receipt changes are engine labels only.

## Gates and installed consumer behavior

Clean npm ci, build, 1,990 workspace tests, test typechecking, 457 default and
76 declarative conformance checks, 34 JSON schemas, lint, lockfile/package/
version/index/release checks and documentation build passed.

Separate tarball and registry installations used real installed packages rather
than workspace links. Both verified:

- Core/browser property assembly and CLI/API equality for all 12 synthetic rows,
  20 coverage cells and four explicit-zero declarations.
- Typed API and CLI refusals, unchanged input documents and plans, and
  `coverage: declared_complete` retained in the result.
- Existing dated engine output: synthetic XNPV 197078.23 at the explicit 0.08
  discount rate and XIRR 0.54739; acquisition anchor 2026-07-01.
- Existing lease-up projection, zero-valued calculation context, CLI help,
  signing exports and batch indexing.

The [synthetic workflow](../PROPERTY_CASH_FLOW_WORKFLOW.md) reproduces the
candidate and pinned metrics from a checkout. Every economic input is labeled
fictional engineering data. Real-deal validation remains the owner's next step;
this release does not establish speculative leasing or Argus parity.
