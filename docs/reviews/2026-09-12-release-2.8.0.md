# Release 2.8.0 verification — 2026-09-12

Release tag **v2.8.0** points to `f0d8642457c7bdf50dc104e67c859a7c9f109f73`. The
[trusted-publishing workflow](https://github.com/UWMD-OSP/UW-Markdown/actions/runs/34712529152)
completed successfully. Registry metadata and a clean npm installation confirm:

| Package | Published version |
|---|---|
| @uwmd/core | 2.8.0 |
| @uwmd/cli | 2.8.0 |
| @uwmd/signing | 0.2.12 |
| @uwmd/batch | 0.8.7 |

Installed core reports Protocol **2.11.0** and Format **2.0**. The same release
keeps standalone Excel 0.9.0, report 0.8.12 and both reference modules 0.1.0
unpublished; the workflow intentionally publishes only the four packages above.

## Evidence

Release preparation used a clean lockfile installation and passed build,
1,931 workspace tests, test typechecking, 441 default and 76 declarative
conformance checks, 31 schemas, lint, lockfile/package/version/index/release
checks and docs production build. PR CI and the release workflow also passed.

Four packed tarballs were installed outside the repository before publication.
After publication, a separate directory installed the four exact versions
from npm. Both runs checked real package directories (no workspace symlinks),
core and browser projection, explicit calculation context preserving zero,
CLI projection and typed refusal JSON, help, signing exports, batch indexing
and source-byte preservation.

The existing lease-up fixture projected exact amounts **118475** and **123425**.
Its semantic source digest was
`sha256:1ecc661844d5597f15387ab90d13a13a1afce00f1936cd24554bb1404af6fab9`.
No financial values, formulas or calculation digests changed in the release
repin; only three receipt engine-version labels advanced from 2.7.0 to 2.8.0.

## Reproduction

Install `@uwmd/core@2.8.0 @uwmd/cli@2.8.0 @uwmd/signing@0.2.12 @uwmd/batch@0.8.7`
in an empty directory. Run the installed `uwmd project-lease-up` against
`conformance/lease-up/valid-value-add-turnover/deal.uwx.md` and
`docs/examples/lease-up-cash-dates.json` from a checkout of the release tag.
Compare the bindings/digest above and verify that the source file is unchanged.
