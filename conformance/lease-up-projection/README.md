# Explicit lease-up cash-flow projection

RFC 0044 fixture/expected pairs, run by the default `lease-up-projection` suite.
Each source intentionally derives from the RFC 0008 legacy-reader fixture;
Format 2.0 wrapped payload coverage also lives in the API acceptance tests.
Expected copied amounts and dates are independently authored, not regenerated
from adapter output. Successful cases additionally verify source digest and
immutability. Failure cases retain the verifier/structural diagnostic vocabulary.
