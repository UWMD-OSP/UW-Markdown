* text=auto eol=lf

*.bat text eol=crlf
*.cmd text eol=crlf

# Digest-pinned package fixtures: manifest.json records a sha256 over the exact
# bytes on disk, so these payloads must never be EOL-translated on checkout.
examples/standalone/package/** -text
