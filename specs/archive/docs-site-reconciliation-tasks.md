# Documentation reconciliation task matrix

Derived from [the contract](docs-site-reconciliation.md). The earlier completed matrix is
[archived](protocol-1.3-hardening-tasks.md).

- [x] Verify the claims, correct stale documentation and site metadata, replace
  the RFC copy list with discovery, and commit after build, tests, conformance,
  schema validation, lint, lockfile/package checks, test typechecking, version/index
  verification, and the docs-site build pass.
