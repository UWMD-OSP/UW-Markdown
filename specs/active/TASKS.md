# Command-line calculation context tasks

- [ ] In flight: finish context transport regression/documentation, pass every
  required gate and commit the completed stage.

Owner explicitly authorized resumption after the repeated-test-failure pause.
The refinement assertion now compares each endpoint with calc at the existing
six-decimal boundary. No interval arithmetic or engine tolerance changes.
PR #184 remains stacked on open #183; #182 is its preceding dependency.
